//! The SDK calls both kgsim modules make, over `wirepod_vector::sdk_client`.
//!
//! Go reaches these through `robot.Conn` on the vector SDK. They have no Go
//! file of their own, so they sit here rather than in either translation.
