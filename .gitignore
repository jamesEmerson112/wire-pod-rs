target/
.env
spikes/**/vendor/
*.log
dump.txt
# `docs/` holds the tracked documents listed below next to scratch notes, which are
# not tracked. `docs/*` rather than `docs/` so that git still descends into the
# directory and the negations can take effect.
docs/*
!docs/translation.md
!docs/robot-api.md
!docs/robot-api.html
!docs/pending-upstream.md
!docs/archive/
.gstack/
